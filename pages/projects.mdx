import { withRouter } from "next/router";
import BlogPost from "../components/layouts/blog-post";
import Page from "../components/layouts/page";
import Projects from "../components/projects";

export const meta = {
  title: "My Projects",
  summary: "List of projects I'm currently working on."
};

export default withRouter(({ children, router }) => (
  <Page path={router.pathname} meta={meta}>
    {children}
  </Page>
));

<Projects list={[
    { name: "DNS Check", description: "DNS lookup web tool.", link: "https://dnscheck.app", },
    { name: "Pa11y", description: "Tool to create accessible color palettes.", link: "https://pa11y.now.sh", },
    { name: "Next Authentication", description: "Authentication & Authorization library for the Next.js framework.", link: "https://github.com/j0lv3r4/next-authentication" },
]} />

